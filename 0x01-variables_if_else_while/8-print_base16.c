#include<stdio.h>
#include<stdlib.h>

/**
 * main - Program entry point
 *
 * Program prints base16 numbers in lower case.
 *
 * Return: Always 0
 */
int main(void)
{
	int m;

	for (m = 48; m < 58; m++)
		putchar (m);

	for (m = 97; m < 103; m++)
		putchar (ch);

	putchar ('\n');
	return (0);
}
